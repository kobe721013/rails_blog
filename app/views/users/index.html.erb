<p id="notice"><%= notice %></p>

<h1>Users</h1>

<%= paginate @users, theme:'twitter-bootstrap-3', :pagination_class => "pagination-small pagination-centered"%>
<table class="table table-hover table-condensed table-bordered">
  <thead>
    <tr>
      <th>Name</th>
      <th>Email</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <!-- 我要render一部份(partial)的頁面
    這個頁面的實際檔名叫'_user.html.erb'
    但我要一直重複用這個頁面, 因為我的資料很多筆
    所以我給一個參數collection:值是@users(複數)
    這個users可以取出很多個user的變數給partial頁面使用
    -->
    <%= render partial:'user', collection:@users, as: :user%>
    <!--有了上面這一行就可以在抽出部分頁面邏輯
    <% @users.each do |user| %>
      <tr>
        <td><%= user.name %></td>
        <td><%= user.email %></td>
        <td><%= link_to 'Show', user, class:"btn btn-success" %></td>
        <td><%= link_to 'Edit', edit_user_path(user),class:"btn btn-warning"%></td>
        <td><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' }, class:"btn btn-danger"%></td>
      </tr>
    <% end %>
  -->
  </tbody>
</table>

<br>

<%= link_to 'New User', new_user_path, class:"btn btn-primary" %>
